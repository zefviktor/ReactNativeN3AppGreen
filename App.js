import React from 'react';
// import type {Node} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StyleSheet,
  Text,
  // View,
} from 'react-native';

function App() {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Подписка</Text>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  
});

export default App;
